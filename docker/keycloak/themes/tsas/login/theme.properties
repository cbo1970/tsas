parent=keycloak.v2
import=common/keycloak

# Inherit the modern keycloak.v2 login stylesheet (css/styles.css, resolved from
# the parent theme) and append the TSaS brand overrides (css/tsas.css).
styles=css/styles.css css/tsas.css
